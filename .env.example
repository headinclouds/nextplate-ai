# Cloudinary Configuration (for production image storage)
# Sign up at: https://cloudinary.com/users/register_free
CLOUDINARY_CLOUD_NAME=your_cloud_name_here
CLOUDINARY_API_KEY=your_api_key_here
CLOUDINARY_API_SECRET=your_api_secret_here

# Node Environment
NODE_ENV=development
# For production: NODE_ENV=production

# Optional: SQLite file path (useful for mounted persistent volumes)
# Example for Railway volume: SQLITE_PATH=/data/meals.db
SQLITE_PATH=meals.db
